/*
*********** While Loop*********
-> checks condition before loop code block.
-> A  while-loop always checks the condition before it execute the code block.
*/

Integer num = 40;

while(num>=0)
{
    System.debug('Current value of num is -'+num);
    num--;
}